apply plugin: 'maven-publish'

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId "com.github.SheTieJun.PlayerKit"
                from components.release
                artifactId = 'videoKit-tencent'
                version = '1.0.0'
            }
        }
    }
}

