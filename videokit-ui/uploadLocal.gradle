apply plugin: 'maven-publish'

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId "com.github.SheTieJun.PlayerKit"
                from components.release
                artifactId = 'videokit-ui'
                version = '1.0.0'
            }
        }
    }
}

